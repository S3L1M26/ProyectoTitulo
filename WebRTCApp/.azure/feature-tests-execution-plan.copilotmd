# 🚀 Plan de Ejecución - Feature Tests (Fase 3)

**Fecha de inicio**: 24 de octubre de 2025  
**Proyecto**: WebRTCApp - Sistema de Emparejamiento Estudiante-Mentor  
**Estado**: ⏳ PLANIFICADO (pendiente de ejecución)

---

## 📊 **CONTEXTO ACTUAL**

### **Estado de Tests Existentes**:
- ✅ **Unit Tests**: 110 tests, 207 assertions, 100% pasando
- ✅ **Cobertura Unit**: 20.48% methods, 14.29% classes
- ✅ **Feature Tests Auth**: 6 tests (Laravel Breeze - YA EXISTEN)

### **Objetivo de Fase 3**:
- 🎯 **Feature Tests**: Crear 35-40 tests adicionales
- 🎯 **Cobertura Total**: Alcanzar 35-38% method coverage
- 🎯 **Tests Totales**: ~145-150 tests (Unit + Feature)
- 🎯 **Tiempo Estimado**: 9-11 horas según FEATURE_TESTING_PLAN.md

---

## 🗂️ **ESTRUCTURA DE IMPLEMENTACIÓN**

```
tests/Feature/
├── Auth/ ✅ YA EXISTE (6 tests de Laravel Breeze)
│   ├── RegistrationTest.php
│   ├── AuthenticationTest.php
│   ├── EmailVerificationTest.php
│   ├── PasswordResetTest.php
│   ├── PasswordUpdateTest.php
│   └── PasswordConfirmationTest.php
│
├── Models/ 📝 NUEVO (13-16 tests)
│   ├── UserCompletenessTest.php (8-10 tests) 🔥 PRIORIDAD ALTA
│   └── MentorRelationshipsTest.php (5-6 tests) 🎯 PRIORIDAD MEDIA
│
├── Controllers/ 📝 NUEVO (12-16 tests)
│   ├── StudentControllerIntegrationTest.php (6-8 tests) 🔥 PRIORIDAD ALTA
│   └── ProfileControllerIntegrationTest.php (6-8 tests) 🎯 PRIORIDAD MEDIA
│
├── Jobs/ 📝 NUEVO (4-5 tests)
│   └── SendProfileReminderJobIntegrationTest.php (4-5 tests) 🔥 PRIORIDAD ALTA
│
└── Notifications/ 📝 NUEVO (6-8 tests)
    ├── VerifyEmailNotificationTest.php (3-4 tests) 📧 MIGRACIÓN
    └── ProfileIncompleteReminderTest.php (3-4 tests) 📧 MIGRACIÓN
```

---

## 📋 **ORDEN DE EJECUCIÓN (12 PASOS)**

### **🔧 PASO 1: Preparación del Entorno**
**Acción**: Crear estructura de directorios

```bash
mkdir -p tests/Feature/Models
mkdir -p tests/Feature/Controllers
mkdir -p tests/Feature/Jobs
mkdir -p tests/Feature/Notifications
```

**Verificar**: tests/Feature/Auth/ ya existe (6 archivos de Breeze)

**Tiempo estimado**: 5 minutos

---

### **🔥 PASO 2: UserCompletenessTest.php (PRIORIDAD ALTA)**

**Archivo**: `tests/Feature/Models/UserCompletenessTest.php`

**Tests a implementar** (8-10 tests):

1. ✅ `test_student_with_complete_profile_has_100_percent_completeness()`
   - User::factory(['role' => 'student'])
   - Aprendiz con semestre + objetivos
   - 2 AreaInteres attached
   - Assert: percentage = 100, missing_fields = []

2. ✅ `test_student_without_areas_has_partial_completeness()`
   - Aprendiz con semestre + objetivos, SIN áreas
   - Assert: percentage = 60, missing_fields contiene 'Áreas de interés'

3. ✅ `test_student_without_semestre_has_low_completeness()`
   - Aprendiz con objetivos, SIN semestre, SIN áreas
   - Assert: percentage = 25

4. ✅ `test_student_with_empty_profile_has_zero_completeness()`
   - User sin Aprendiz o Aprendiz vacío
   - Assert: percentage = 0, missing_fields = 3 items

5. ✅ `test_mentor_with_complete_profile_has_100_percent_completeness()`
   - Mentor con experiencia (>50 chars), biografía (>100 chars), años_experiencia, disponibilidad
   - 3 AreaInteres attached
   - Assert: percentage = 100

6. ✅ `test_mentor_with_short_experiencia_fails_validation()`
   - Mentor con experiencia <50 chars
   - Assert: missing_fields contiene 'Experiencia'

7. ✅ `test_mentor_with_short_biografia_fails_validation()`
   - Mentor con biografía <100 chars
   - Assert: missing_fields contiene 'Biografía'

8. ✅ `test_mentor_without_areas_has_partial_completeness()`
   - Mentor completo pero SIN áreas de interés
   - Assert: percentage < 100

9. ✅ `test_profile_completeness_weights_are_consistent()`
   - Verificar que suma de porcentajes = 100%

10. ✅ `test_missing_fields_are_translated_correctly()` (OPCIONAL)
    - Verificar nombres de campos en español

**Patrón de código**:
```php
<?php

namespace Tests\Feature\Models;

use Tests\TestCase;
use App\Models\User;
use App\Models\Aprendiz;
use App\Models\Mentor;
use App\Models\AreaInteres;
use Illuminate\Foundation\Testing\RefreshDatabase;

class UserCompletenessTest extends TestCase
{
    use RefreshDatabase;

    /** @test */
    public function student_with_complete_profile_has_100_percent_completeness()
    {
        $user = User::factory()->create(['role' => 'student']);
        
        $aprendiz = Aprendiz::factory()->for($user)->create([
            'semestre' => 5,
            'objetivos' => 'Aprender programación avanzada'
        ]);
        
        $areas = AreaInteres::factory()->count(2)->create();
        $aprendiz->areasInteres()->attach($areas);
        
        $completeness = $user->fresh()->profile_completeness;
        
        $this->assertEquals(100, $completeness['percentage']);
        $this->assertEmpty($completeness['missing_fields']);
        $this->assertCount(3, $completeness['completed_fields']);
    }
    
    // ... más tests siguiendo el patrón
}
```

**Impacto esperado**:
- ✅ Cobertura de User::calculateStudentCompleteness(): 100%
- ✅ Cobertura de User::calculateMentorCompleteness(): 100%
- ✅ Aumento global: +15% en métodos

**Tiempo estimado**: 2-3 horas

---

### **🔥 PASO 3: StudentControllerIntegrationTest.php (PRIORIDAD ALTA)**

**Archivo**: `tests/Feature/Controllers/StudentControllerIntegrationTest.php`

**Tests a implementar** (6-8 tests):

1. ✅ `test_authenticated_student_can_access_dashboard()`
   - actingAs(student) → get('/student/dashboard')
   - assertStatus(200), assertViewIs('student.dashboard')

2. ✅ `test_unauthenticated_user_cannot_access_student_dashboard()`
   - get('/student/dashboard') sin autenticación
   - assertRedirect('/login')

3. ✅ `test_mentor_cannot_access_student_dashboard()`
   - actingAs(mentor) → get('/student/dashboard')
   - assertStatus(403) o assertRedirect()

4. ✅ `test_student_sees_mentors_matching_their_areas()`
   - Crear estudiante con AreaInteres 'PHP'
   - Crear 2 mentores: uno con 'PHP', otro con 'JavaScript'
   - Assert: see('Mentor PHP'), dontSee('Mentor JS')

5. ✅ `test_student_sees_only_available_mentors()`
   - Crear mentor disponible (disponible_ahora = true)
   - Crear mentor NO disponible (disponible_ahora = false)
   - Assert: solo ve el disponible

6. ✅ `test_mentor_suggestions_are_cached_for_performance()`
   - Verificar que Cache::remember() se llama
   - Segunda llamada NO ejecuta query

7. ✅ `test_student_without_areas_sees_all_mentors()` (OPCIONAL)
   - Estudiante sin áreas de interés
   - Ve todos los mentores disponibles

8. ✅ `test_dashboard_shows_profile_completeness_message()` (OPCIONAL)
   - Estudiante con perfil incompleto
   - Assert: see mensaje de completitud

**Patrón de código**:
```php
<?php

namespace Tests\Feature\Controllers;

use Tests\TestCase;
use App\Models\User;
use App\Models\Aprendiz;
use App\Models\Mentor;
use App\Models\AreaInteres;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Support\Facades\Cache;

class StudentControllerIntegrationTest extends TestCase
{
    use RefreshDatabase;

    /** @test */
    public function student_sees_mentors_matching_their_areas()
    {
        // Crear estudiante con área PHP
        $student = User::factory()->create(['role' => 'student']);
        $aprendiz = Aprendiz::factory()->for($student)->create();
        $area = AreaInteres::factory()->create(['nombre' => 'PHP']);
        $aprendiz->areasInteres()->attach($area);
        
        // Mentor con match
        $matchingMentor = User::factory()->create(['role' => 'mentor', 'name' => 'Mentor PHP']);
        $mentorProfile = Mentor::factory()->for($matchingMentor)->create(['disponible_ahora' => true]);
        $mentorProfile->areasInteres()->attach($area);
        
        // Mentor sin match
        $otherMentor = User::factory()->create(['role' => 'mentor', 'name' => 'Mentor JS']);
        
        $response = $this->actingAs($student)->get('/student/dashboard');
        
        $response->assertStatus(200);
        $response->assertSee('Mentor PHP');
        $response->assertDontSee('Mentor JS');
    }
}
```

**Impacto esperado**:
- ✅ Cobertura de StudentController: +10%
- ✅ Tests E2E de flujo crítico de búsqueda

**Tiempo estimado**: 2-2.5 horas

---

### **🔥 PASO 4: SendProfileReminderJobIntegrationTest.php (PRIORIDAD ALTA)**

**Archivo**: `tests/Feature/Jobs/SendProfileReminderJobIntegrationTest.php`

**Tests a implementar** (4-5 tests):

1. ✅ `test_job_sends_notification_to_incomplete_student_profile()`
   - User estudiante con perfil incompleto
   - Notification::fake()
   - Dispatch job
   - assertSentTo(ProfileIncompleteReminder)

2. ✅ `test_job_calculates_correct_profile_data_from_database()`
   - User con semestre completo (35%)
   - Verificar que notification recibe ['percentage' => 35]

3. ✅ `test_job_sends_notification_to_incomplete_mentor_profile()`
   - User mentor con perfil incompleto
   - assertSentTo(ProfileIncompleteReminder)

4. ✅ `test_job_includes_missing_fields_in_notification()`
   - Verificar que notification recibe missing_fields correctos

5. ✅ `test_job_does_not_send_notification_if_profile_complete()` (OPCIONAL)
   - User con 100% completeness
   - assertNotSentTo()

**Patrón de código**:
```php
<?php

namespace Tests\Feature\Jobs;

use Tests\TestCase;
use App\Models\User;
use App\Models\Aprendiz;
use App\Jobs\SendProfileReminderJob;
use App\Notifications\ProfileIncompleteReminder;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Illuminate\Support\Facades\Notification;

class SendProfileReminderJobIntegrationTest extends TestCase
{
    use RefreshDatabase;

    /** @test */
    public function job_sends_notification_to_incomplete_student_profile()
    {
        Notification::fake();
        
        $user = User::factory()->create(['role' => 'student', 'email_verified_at' => now()]);
        Aprendiz::factory()->for($user)->create([
            'semestre' => null,
            'objetivos' => null
        ]);
        
        $job = new SendProfileReminderJob($user);
        $job->handle();
        
        Notification::assertSentTo($user, ProfileIncompleteReminder::class);
    }
}
```

**Impacto esperado**:
- ✅ Cobertura de SendProfileReminderJob: +5%
- ✅ Tests de integración con BD real

**Tiempo estimado**: 1-1.5 horas

---

### **🎯 PASO 5: MentorRelationshipsTest.php (PRIORIDAD MEDIA)**

**Archivo**: `tests/Feature/Models/MentorRelationshipsTest.php`

**Tests a implementar** (5-6 tests):

1. ✅ `test_mentor_can_have_multiple_areas_of_interest()`
   - Mentor::factory(), AreaInteres::factory(3)
   - attach() relación M2M
   - assertCount(3)

2. ✅ `test_mentor_stars_rating_is_formatted_correctly()`
   - Mentor con calificacionPromedio = 4.7
   - assertEquals('4.7 ⭐', $mentor->stars_rating)

3. ✅ `test_mentor_rating_percentage_is_calculated_correctly()`
   - calificacionPromedio = 3.5
   - assertEquals(70, $mentor->rating_percentage)

4. ✅ `test_mentor_with_null_rating_shows_default_stars()`
   - calificacionPromedio = null
   - assertEquals('Sin calificación', $mentor->stars_rating)

5. ✅ `test_mentor_belongs_to_user()`
   - Verificar relación belongsTo() funcional

6. ✅ `test_areas_interes_can_be_detached()` (OPCIONAL)
   - attach() y luego detach()
   - assertCount(0)

**Tiempo estimado**: 1.5-2 horas

---

### **🎯 PASO 6: ProfileControllerIntegrationTest.php (PRIORIDAD MEDIA)**

**Archivo**: `tests/Feature/Controllers/ProfileControllerIntegrationTest.php`

**Tests a implementar** (6-8 tests):

1. ✅ `test_student_can_update_their_profile()`
   - actingAs(student) → put('/profile/aprendiz')
   - assertRedirect(), assertDatabaseHas()

2. ✅ `test_mentor_can_update_their_profile()`
   - actingAs(mentor) → put('/profile/mentor')
   - assertDatabaseHas('mentores')

3. ✅ `test_mentor_can_toggle_disponibilidad()`
   - post('/profile/mentor/toggle-disponibilidad')
   - Verificar cambio en BD

4. ✅ `test_profile_update_validates_required_fields()`
   - Enviar datos inválidos
   - assertSessionHasErrors()

5. ✅ `test_user_can_delete_their_account()`
   - delete('/profile')
   - assertDatabaseMissing('users')

6. ✅ `test_get_areas_interes_returns_json()`
   - get('/profile/areas-interes')
   - assertJson()

**Tiempo estimado**: 2-2.5 horas

---

### **📧 PASO 7: VerifyEmailNotificationTest.php (MIGRACIÓN)**

**Archivo**: `tests/Feature/Notifications/VerifyEmailNotificationTest.php`

**Tests a implementar** (3-4 tests migrados):

1. ✅ `test_verification_email_contains_correct_url()`
   - User::factory(['email_verified_at' => null])
   - toMail() con usuario real
   - assertStringContainsString('verify-email')

2. ✅ `test_verification_email_has_correct_subject()`
   - assertEquals('Verificar Correo Electrónico')

3. ✅ `test_verification_url_includes_user_id_and_hash()`
   - assertStringContainsString(user->id)

**Basado en**: UNIT_TO_FEATURE_MIGRATION.md

**Tiempo estimado**: 45 minutos

---

### **📧 PASO 8: ProfileIncompleteReminderTest.php (MIGRACIÓN)**

**Archivo**: `tests/Feature/Notifications/ProfileIncompleteReminderTest.php`

**Tests a implementar** (3-4 tests migrados):

1. ✅ `test_student_receives_profile_reminder_email()`
   - Notification::fake()
   - User con perfil incompleto
   - assertSentTo()

2. ✅ `test_profile_reminder_email_contains_percentage()`
   - Mail::fake()
   - Verificar que render() contiene '45%'

3. ✅ `test_profile_reminder_content_differs_by_role()`
   - Estudiante vs Mentor
   - Contenido diferente

**Basado en**: UNIT_TO_FEATURE_MIGRATION.md

**Tiempo estimado**: 45 minutos

---

### **✅ PASO 9: Ejecutar Suite Completa de Feature Tests**

**Comando**:
```bash
docker-compose exec app php artisan test tests/Feature --exclude-group=auth
```

**Verificaciones**:
- ✅ Todos los tests nuevos pasan
- ✅ 0 fallos, 0 errores
- ✅ Tiempo de ejecución aceptable (<5 minutos)

**Si hay errores**:
1. Revisar factories (User, Aprendiz, Mentor, AreaInteres)
2. Verificar rutas en web.php
3. Comprobar RefreshDatabase trait

**Tiempo estimado**: 30 minutos (incluye debugging)

---

### **📊 PASO 10: Generar Reporte de Cobertura COMPLETO**

**Comando**:
```bash
docker-compose exec app vendor/bin/phpunit --coverage-text --colors=never
```

**Métricas a capturar**:
- **Methods Coverage**: Objetivo 35-38% (vs 20.48% actual)
- **Classes Coverage**: Objetivo 25-30% (vs 14.29% actual)
- **Lines Coverage**: Objetivo 10-15% (vs 4.45% actual)

**Comparativa esperada**:

| Métrica | Unit Only | Unit + Feature | Mejora |
|---------|-----------|----------------|--------|
| Tests | 110 | ~145-150 | +32-36% |
| Methods | 20.48% | 35-38% | +71-85% |
| Classes | 14.29% | 25-30% | +75-110% |
| Lines | 4.45% | 10-15% | +124-237% |

**Tiempo estimado**: 15 minutos

---

### **📝 PASO 11: Actualizar Documentación**

**Archivo**: `TESTING_IMPLEMENTATION_RESULTS.md`

**Sección a añadir**: "Fase 3 - Feature Tests Implementation"

**Contenido**:
- Total de Feature Tests creados
- Distribución por categoría (Models, Controllers, Jobs, Notifications)
- Cobertura alcanzada vs objetivo
- Tests migrados de Unit a Feature
- Lecciones aprendidas

**Tiempo estimado**: 30 minutos

---

### **🎉 PASO 12: Generar Evidencia Final Consolidada**

**Archivo**: `EVIDENCIA_TESTING.md`

**Actualizar con**:
- Sección "Fase 3 - Feature Tests"
- Output completo de ejecución de Feature Tests
- Reporte de cobertura completo (Unit + Feature)
- Tabla comparativa 3 fases (inicial → Unit → Unit+Feature)
- Conclusión final del proyecto

**Tiempo estimado**: 45 minutos

---

## 📈 **MÉTRICAS DE ÉXITO**

### **Tests Totales**:
- ✅ Unit Tests: 110 (completado)
- 🎯 Feature Tests: 35-40 (objetivo)
- 🎯 **Total**: ~145-150 tests

### **Cobertura**:
- ✅ Methods (Unit only): 20.48%
- 🎯 Methods (Unit + Feature): **35-38%** ✨
- 🎯 Classes: **25-30%**
- 🎯 Lines: **10-15%**

### **Componentes con 100% Cobertura** (proyectado):
- ✅ SendProfileReminderJob (ya en 100%)
- ✅ Mentor (ya en 100%)
- ✅ AreaInteres (ya en 100%)
- ✅ ResetPasswordNotification (ya en 100%)
- ✅ AppServiceProvider (ya en 100%)
- 🎯 User (calculateStudentCompleteness, calculateMentorCompleteness)
- 🎯 Aprendiz (relaciones)

---

## ⏱️ **ESTIMACIÓN DE TIEMPO TOTAL**

| Paso | Descripción | Tiempo |
|------|-------------|--------|
| 1 | Crear directorios | 5 min |
| 2 | UserCompletenessTest (8-10 tests) | 2-3 h |
| 3 | StudentControllerIntegrationTest (6-8 tests) | 2-2.5 h |
| 4 | SendProfileReminderJobIntegrationTest (4-5 tests) | 1-1.5 h |
| 5 | MentorRelationshipsTest (5-6 tests) | 1.5-2 h |
| 6 | ProfileControllerIntegrationTest (6-8 tests) | 2-2.5 h |
| 7 | VerifyEmailNotificationTest (3-4 tests) | 45 min |
| 8 | ProfileIncompleteReminderTest (3-4 tests) | 45 min |
| 9 | Ejecutar suite + debugging | 30 min |
| 10 | Generar cobertura | 15 min |
| 11 | Actualizar documentación | 30 min |
| 12 | Evidencia final | 45 min |
| **TOTAL** | **12 pasos** | **9-11 horas** |

---

## 🎯 **PRIORIZACIÓN**

### **Si tienes tiempo limitado, implementa en este orden**:

1. **CRÍTICO** (4-5 horas):
   - ✅ PASO 2: UserCompletenessTest (lógica crítica de negocio)
   - ✅ PASO 3: StudentControllerIntegrationTest (flujo principal)
   - ✅ PASO 4: SendProfileReminderJobIntegrationTest (notificaciones)

2. **IMPORTANTE** (3-4 horas):
   - ✅ PASO 5: MentorRelationshipsTest
   - ✅ PASO 6: ProfileControllerIntegrationTest

3. **COMPLEMENTARIO** (2 horas):
   - ✅ PASO 7-8: Migraciones de Notifications
   - ✅ PASO 9-12: Validación y documentación

---

## ⚠️ **CONSIDERACIONES TÉCNICAS**

### **Factories Requeridos**:
Verificar que existen en `database/factories/`:
- ✅ UserFactory.php (debe existir)
- 🔍 AprendizFactory.php (verificar)
- 🔍 MentorFactory.php (verificar)
- 🔍 AreaInteresFactory.php (verificar)

### **Rutas Requeridas**:
Verificar en `routes/web.php`:
- ✅ `/student/dashboard` (debe existir)
- ✅ `/profile/aprendiz` (PUT)
- ✅ `/profile/mentor` (PUT)
- ✅ `/profile/mentor/toggle-disponibilidad` (POST)
- ✅ `/profile/areas-interes` (GET)

### **Base de Datos de Testing**:
```xml
<!-- phpunit.xml - ya configurado -->
<env name="DB_CONNECTION" value="sqlite"/>
<env name="DB_DATABASE" value=":memory:"/>
```

### **Trait Obligatorio en TODOS los Feature Tests**:
```php
use Illuminate\Foundation\Testing\RefreshDatabase;

class MiFeatureTest extends TestCase
{
    use RefreshDatabase; // ⚠️ OBLIGATORIO
}
```

---

## ✅ **CHECKLIST PRE-EJECUCIÓN**

Antes de empezar, verificar:

- [ ] Factories existen y funcionan (User, Aprendiz, Mentor, AreaInteres)
- [ ] Rutas de controllers están definidas en web.php
- [ ] RefreshDatabase funciona correctamente
- [ ] Migraciones están actualizadas (`php artisan migrate:fresh`)
- [ ] 110 Unit Tests siguen pasando (no regresión)
- [ ] 6 Feature Tests de Auth funcionan (Laravel Breeze)

---

## 🚀 **SIGUIENTE PASO**

Una vez revisado este plan, proceder con:

```bash
# PASO 1: Crear directorios
mkdir -p tests/Feature/Models
mkdir -p tests/Feature/Controllers
mkdir -p tests/Feature/Jobs
mkdir -p tests/Feature/Notifications

# PASO 2: Empezar con UserCompletenessTest.php (PRIORIDAD ALTA)
```

---

**Generado**: 24 de octubre de 2025  
**Basado en**: FEATURE_TESTING_PLAN.md + UNIT_TO_FEATURE_MIGRATION.md  
**Estado**: ⏳ LISTO PARA EJECUTAR
